import {
  strings,
  parserFactory,
} from '../Parser/parserFactory';

/*const gatelyparse
const htmlParse  = require('../PEG/HtmlParser').parse;
jest.mock('../PEG/HtmlParser');
const sugarParse = require('../PEG/SugarParser').parse;
jest.mock('../PEG/SugarParser');

describe('parserFactory unit tests.', () => {
  beforeEach(() => {

  });
});